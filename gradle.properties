###############################################################################
# Project-wide Gradle settings — ONE key=value per line, comments on their own
###############################################################################

# --- Java / Kotlin -----------------------------------------------------------
kotlin.jvm.target=17
kotlin.jvm.target.validation.mode=warning
kotlin.incremental=true

# Heap for Gradle (3 GB) and Kotlin daemon (2 GB)
org.gradle.jvmargs=-Xmx3g -XX:+UseParallelGC -Dfile.encoding=UTF-8 -Dkotlin.daemon.jvm.options="-Xmx2048m"

# --- AndroidX & SDK ----------------------------------------------------------
android.useAndroidX=true
android.enableJetifier=false
android.suppressUnsupportedCompileSdk=35

# --- Performance toggles -----------------------------------------------------
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.configureondemand=true
org.gradle.workers.max=4
org.gradle.caching=true
org.gradle.configuration-cache=true

# --- Console output ----------------------------------------------------------
# Use plain output so Codex and CI logs render cleanly
org.gradle.console=plain
