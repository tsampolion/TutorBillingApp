###############################################################################
# Project-wide Gradle settings
###############################################################################

# ---- Java & Kotlin ----------------------------------------------------------
kotlin.jvm.target=17
kotlin.jvm.target.validation.mode=warning
kotlin.code.style=official            # keep IntelliJ / ktlint happy
kotlin.incremental=true               # speed: compile only changed files  :contentReference[oaicite:0]{index=0}

# More heap and a GC better suited to parallel builds
org.gradle.jvmargs=-Xmx3g -XX:+UseParallelGC -Dfile.encoding=UTF-8 \
                   -Dkotlin.daemon.jvm.options="-Xmx2048m"  :contentReference[oaicite:1]{index=1}

# ---- AndroidX ---------------------------------------------------------------
android.useAndroidX=true
android.enableJetifier=false
android.suppressUnsupportedCompileSdk=35

# ---- Build-speed flags ------------------------------------------------------
org.gradle.daemon=true                # keep a warm daemon between builds  :contentReference[oaicite:2]{index=2}
org.gradle.parallel=true              # compile modules side-by-side       :contentReference[oaicite:3]{index=3}
org.gradle.configureondemand=true     # configure only projects you need   :contentReference[oaicite:4]{index=4}
org.gradle.workers.max=4              # adjust ≤ CPU cores
org.gradle.caching=true               # reuse task outputs                 :contentReference[oaicite:5]{index=5}
org.gradle.configuration-cache=true   # skip repeated project config       :contentReference[oaicite:6]{index=6}

# ---- Console ----------------------------------------------------------------
org.gradle.console=plain              # cleaner CI / Codex logs
